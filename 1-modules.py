import calc # import the module as a whole
from calc import div # import only specific functions from the module

print(calc.sum(10, 2))
print(calc.sub(10, 6))
print(calc.mult(10, 6))
print(calc.div(10, 2))